<% badge = local_assigns[:badge] %>
<% if value.present? || badge %>
  <%= value %>
  <%= render badge if badge %>
<% else %>
  <span class="fr-text--mention-grey"><%= t('shared.not_provided') %></span>
<% end %>
